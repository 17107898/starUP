// document.addEventListener("DOMContentLoaded", function() {
//     const image = document.getElementById('gallery-image'); // Certifique-se que a imagem tem esse ID
//     const prevButton = document.querySelector('.prev');
//     const nextButton = document.querySelector('.next');

//     // Verifique se a imagem existe
//     if (image && prevButton && nextButton) {
//         // Pegamos a largura da imagem incluindo a borda
//         const imageWidth = image.offsetWidth;

//         // Calculamos a posição dos botões
//         prevButton.style.left = `-${prevButton.offsetWidth + 10}px`; // 10px fora da moldura
//         nextButton.style.right = `-${nextButton.offsetWidth + 10}px`; // 10px fora da moldura
//     } else {
//         console.error("Imagem ou botões não encontrados.");
//     }
// });
